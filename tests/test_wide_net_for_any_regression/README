Goal: wide net to catch future regressions in many parts of the pipeline.

The data was generated by an LLM and corrected manually to follow some of the assumptions we can
make about the upstream data.
