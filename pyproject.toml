[project]
name = "kanta-lab-preprocessing"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pandas>=3.0.2",
]

[dependency-groups]
dev = [
    "ipython>=9.12.0",
    "pytest>=9.0.3",
]
